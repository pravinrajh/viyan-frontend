# Canonical Flutter app

This `MD New` tree is the canonical Viyan Flutter implementation.

The parent `MD EAO/` project root `lib/` and `test/` are synced from this tree.
Prefer running from either directory after sync; keep changes in this tree first if both exist.
