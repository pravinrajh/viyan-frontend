/// Placeholder model so CRM follows the feature folder contract.
class CrmOverview {
  const CrmOverview({required this.message, required this.openFollowUps});

  final String message;
  final int openFollowUps;
}
